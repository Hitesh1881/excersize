// Given the array of numbers:
// let numbers = [1, 2, 3, 4, 5];
// Use the `forEach` method to log each element in the array to the console.


let numbers = [1, 2, 3, 4, 5];

numbers.forEach(function (element) {

    console.log(element);
})