// Given an array of numbers:

// let numbers = [2, 5, 8, 11, 14];

// Use a `for` loop to log each element in the array to the console.


let numbers = [2, 5, 8, 11, 14];

for (let i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);
}
