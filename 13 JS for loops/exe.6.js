// Given a string:

// let word = "JavaScript";

// Use a `for` loop to log each character of the string to the console.


let word = "JavaScript";

for (let i = 0; i < word.length; i++) {
    console.log(word[i]);
}
