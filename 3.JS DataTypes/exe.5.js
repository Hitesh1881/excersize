// Given the following array:

// let numbers = [1, 2, 3, 4, 5];

// Use array methods to add a new number to the end of the array, remove the first number, and log the modified array.


let numbers = [1, 2, 3, 4, 5];

numbers.push(6);

numbers.shift();

console.log(numbers);