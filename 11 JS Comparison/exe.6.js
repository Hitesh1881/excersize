// Create a function called `findMax` that takes three numbers as parameters and returns the maximum value.


function findMax(num1, num2, num3) {
    return Math.max(num1, num2, num3);
}

let maxNumber = findMax(7, 15, 4);
console.log("The maximum number is:", maxNumber);
