// Given the array of strings:

// let fruits = ["banana", "apple", "orange", "grape", "kiwi"];

// Use the `sort` method to sort the strings in alphabetical order. Log the sorted array.


let fruits = ["banana", "apple", "orange", "grape", "kiwi"];

fruits.sort();

console.log(fruits);