// Given the array of numbers:

// let numbers = [10, 3, 8, 1, 7];

// Use the `sort` method to sort the numbers in ascending order. Log the sorted array to the console.


let numbers = [10,3,8,1,7];

numbers.sort((a, b) => a - b);

console.log(numbers);
