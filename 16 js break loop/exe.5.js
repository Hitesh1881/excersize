// Create a function called `findEven` that takes an array of numbers as a parameter. Use a `for` loop to find the first even number in the array.
//  Log the result and break out of the loop.

function findEven(numbers){
    for (let i=0;i<numbers.length;i++){
        if(numbers[i] % 2===0){
            console.log("First even number in the array is :",);
        }
    }
}