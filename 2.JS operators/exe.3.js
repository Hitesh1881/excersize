// Create two variables, str1 and str2, and assign them string values. Use the concatenation operator (+) to concatenate the strings and log the result.

let str1 = "Koffee";
let str2 = "Kodes";

let result = str1 + str2;

console.log(result);