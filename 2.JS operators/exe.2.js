// Declare a variable counter and set it to 0. Use the increment and decrement operators to modify the counter variable. Log the variable after each operation.


let counter = 0;

counter ++;
console.log("After increment:",counter);

counter --;
console.log("After decrement:",counter);